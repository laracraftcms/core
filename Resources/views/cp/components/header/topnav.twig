<nav class="navbar navbar-light bg-faded">
    <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#topnav">
        &#9776;
    </button>
    <div class="collapse navbar-toggleable-xs" id="topnav">
        <a class="navbar-brand" href="{{ url('/') }}">Laracraft</a>
        <ul class="nav navbar-nav">
            <li class="nav-item">
                <a class="nav-link" href="{{ route(config('laracraft-core.cp_root')~'.index') }}">{{ trans('core::routes.'~ config('laracraft-core.cp_root')~'.index.menu') }}</a>
            </li>
			<li class="nav-item">
				<a class="nav-link" href="{{ route(config('laracraft-core.cp_root')~'.entry.index') }}">{{ trans('core::routes.'~ config('laracraft-core.cp_root')~'.entry.index.menu') }}</a>
			</li>
        </ul>
        <ul class="nav navbar-nav pull-right">
            <li class="nav-item">
                <a class="nav-link" href="{{ route(config('laracraft-core.cp_root')~'.configure.index') }}">{{ trans('core::routes.'~ config('laracraft-core.cp_root')~'.configure.index.menu') }}</a>
            </li>
			<li class="nav-item dropdown">
				<a class="nav-link dropdown-toggle" id="profileMenu" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">{{ auth_user().name }}<img class="profile-avatar rounded-circle" src="https://www.gravatar.com/avatar/{{ md5(auth_user().email) }}?s=40&d=mm"></a>
				<div class="dropdown-menu" aria-labelledby="profileMenu">
					<a class="dropdown-item" href="{{ route('logout') }}">{{ 'core::auth.logout'|trans }}</a>
				</div>
			</li>
        </ul>
    </div>
</nav>